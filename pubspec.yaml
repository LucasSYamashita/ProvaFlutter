name: seu_nome_do_projeto
description: Um novo aplicativo Flutter.

publish_to: 'none' # Remove essa linha se você planeja publicar.

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.3.2
  sqflite: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
